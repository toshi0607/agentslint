# Architecture

acme-app is a single-page app. This file exists, so the `@docs/architecture.md`
import in CLAUDE.md resolves fine. `./docs/style-guide.md` was renamed months ago —
that reference is the one agentslint flags.
