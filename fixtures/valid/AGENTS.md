# AGENTS

This is a valid fixture used to assert zero findings across all rules.

See the [guide](./docs/guide.md) for background. External references like
[the spec](https://example.com/spec) and [anchor](#section) are skipped by
AL001 since they are not relative file paths.

## Commands

```bash
npm run build
npm run test
```

Also see `npm run lint` inline.

## Section

Some closing text.
