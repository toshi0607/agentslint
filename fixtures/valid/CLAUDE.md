# CLAUDE

This is a valid fixture used to assert zero findings across all rules.

Shared rules: @./docs/shared-rules.md

See the [guide](./docs/guide.md) for background.

## Commands

```bash
npm run build
```

Run `npm run lint` before committing.
